# Split central: connects to 2 peripherals (left + right; no trackball in use).
CONFIG_ZMK_SPLIT_BLE_CENTRAL_PERIPHERALS=2

# Needs a connection/bond slot for each peripheral (2) plus each BLE host
# profile (5, ZMK's default profile count). Per ZMK's own dongle docs.
CONFIG_BT_MAX_CONN=7
CONFIG_BT_MAX_PAIRED=7

# USB-powered dongle: never deep sleep, so the display and BLE links stay alive.
CONFIG_ZMK_SLEEP=n

# The dongle is the split central: fetch and proxy the halves' battery levels.
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_FETCHING=y
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_PROXY=y

# Prospector status screen preferences
CONFIG_PROSPECTOR_STATUS_SCREEN_OPERATOR=y
CONFIG_PROSPECTOR_USE_AMBIENT_LIGHT_SENSOR=y

# TEMPORARY: verify the rebuilt firmware pairs cleanly, and characterize the
# ambient light sensor's chip id (0x9f -- not a documented genuine or clone
# APDS9960 id anywhere; probe reads its raw registers to tell a real chip
# variant apart from a bad I2C read). Remove both once resolved.
CONFIG_ZMK_USB_LOGGING=y
CONFIG_ZMK_LOG_LEVEL_DBG=y
CONFIG_TOTEM_APDS_PROBE=y

# Hold early boot logs ~5s so the USB serial can attach before they flush, and
# give the buffer room, so the boot-time pairing sequence + probe are
# reliably capturable.
CONFIG_LOG_PROCESS_THREAD_STARTUP_DELAY_MS=5000
CONFIG_LOG_BUFFER_SIZE=8192
